let A = [25,5,87,4,6,12,1,54,10,46,63,56,8];

nextMassive:
for (let i = 1; i <= n; i++){ // Для всех i...
      for (let j = 1; j < i; j++) { // проверяем, делится ли число..
    if (i % j == 0) continue nextMassive; // если не подходит, берём следующее
  }
allert(A.concat);
}
